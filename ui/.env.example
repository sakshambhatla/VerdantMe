# Supabase (optional — omit for local dev without auth)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Cloud deploy (set on Vercel; omit for local dev — Vite proxy handles /api)
VITE_API_BASE_URL=https://your-render-service.onrender.com/api
VITE_FORCE_MANAGED=true
